package org.jmlspecs.jmlunitng;

public class TestDataClassGenerator {

	public TestDataClassGenerator()
	{
		//Constructor.
	}
	
	protected void printImportStatements()
	{
		// Prints import statements. 
	}
	protected void printClassJavadoc()
	{
	 // Print Java docs.	 
	}
	 protected void generateTestClass()
	 {
		 //Generates test data class by calling other methods.
	 }
	 protected void initializeFixture()
	 {
		 
	 }
	 protected void printDataProvisionMethods()
	 {
		 //Creates an iterator of fixturetype and then iterates over it and calls printDataProvisionCode().
		 //The new iterator generation can be included here.
	 }
	 protected void printDataProvisionCode()
	 {
		 // Find type and variable name.  
	 }
	 private void printIteratorProvisionMethod()
	 {
		 // Prints method based on variable type and name.
	 }
	 /*
	  * There are many other methods which need to be added in this class.
	  */
}
